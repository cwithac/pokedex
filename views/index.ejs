<!DOCTYPE html>
<html>
  <head>
    <% include ../views/partials/head %>
    <title>Pokedex</title>
  </head>
<body >
  <header>
    <a href="/add">Add a Pokemon to the Pokedex</a>
    <p>There are <%=data.length%> Pokemon in the Pokedex</p>
  </header>
  <div class='container'>
      <ul>
          <% for (var i = 0; i < data.length; i++) { %>
              <a href="/<%=i%>"><img id="index" src="<%=data[i].img; %>"></img></a>
         <% } %>
      </ul>
  </div>
  </body>
</html>
